<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:common="clr-namespace:LabelMinusinWPF.Common" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <!--  Converters  -->
    <common:GroupNameToBrushConverter x:Key="GroupNameToBrushConverter" />

    <!--  Icon buttons  -->
    <Style TargetType="materialDesign:PackIcon">
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style BasedOn="{StaticResource MaterialDesignFlatButton}" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
    </Style>

    <DataTemplate x:Key="SharedIconContentTemplate">
        <StackPanel Orientation="Horizontal">
            <materialDesign:PackIcon
                x:Name="PartIcon"
                Width="26"
                Height="26"
                Margin="0,0,6,0"
                VerticalAlignment="Center"
                Kind="{Binding Tag, RelativeSource={RelativeSource AncestorType=ButtonBase}}" />
            <TextBlock
                x:Name="PartText"
                VerticalAlignment="Center"
                FontSize="15"
                FontWeight="Bold"
                Text="{Binding}" />
        </StackPanel>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                <Setter TargetName="PartText" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PartIcon" Property="Margin" Value="0" />
            </DataTrigger>
            <DataTrigger Binding="{Binding}" Value="">
                <Setter TargetName="PartText" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PartIcon" Property="Margin" Value="0" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <Style x:Key="BaseToggleStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="ContentTemplate" Value="{StaticResource SharedIconContentTemplate}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Focusable="False" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1A000000" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="RoyalBlue" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="IconButtonStyle" BasedOn="{StaticResource MaterialDesignFlatButton}" TargetType="Button">
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="materialDesign:RippleAssist.Feedback" Value="RoyalBlue" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="ContentTemplate" Value="{StaticResource SharedIconContentTemplate}" />
    </Style>

    <Style x:Key="IconToggleButtonStyle" BasedOn="{StaticResource BaseToggleStyle}" TargetType="ToggleButton" />
    <Style x:Key="IconRadioButtonStyle" BasedOn="{StaticResource BaseToggleStyle}" TargetType="RadioButton" />

    <!--  Snackbar  -->
    <Style TargetType="{x:Type materialDesign:SnackbarMessage}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextAlignment="Center" TextWrapping="Wrap" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type materialDesign:Snackbar}">
        <Setter Property="MaxHeight" Value="200" />
        <Setter Property="MaxWidth" Value="5000" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="Background" Value="RoyalBlue" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontFamily" Value="微软雅黑" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <!--  Group and label styles  -->
    <Style x:Key="GroupBadgeItemStyle" TargetType="ListBoxItem">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        x:Name="Bd"
                        Margin="4,0"
                        Padding="10,6"
                        Background="{Binding Converter={StaticResource GroupNameToBrushConverter}}"
                        CornerRadius="6"
                        Cursor="Hand"
                        Opacity="0.4">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="{Binding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.7" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="1.0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LabelDotTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="White" />
    </Style>

    <Style x:Key="BaseDotStyle" TargetType="Border">
        <Setter Property="Width" Value="18" />
        <Setter Property="Height" Value="18" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>

    <Style x:Key="DefaultDotStyle" BasedOn="{StaticResource BaseDotStyle}" TargetType="Border">
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Background" Value="{Binding Group, Converter={StaticResource GroupNameToBrushConverter}}" />
        <Style.Resources>
            <Style BasedOn="{StaticResource LabelDotTextStyle}" TargetType="TextBlock" />
        </Style.Resources>
    </Style>

    <Style x:Key="SquareDotStyle" BasedOn="{StaticResource BaseDotStyle}" TargetType="Border">
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="Background" Value="{Binding Group, Converter={StaticResource GroupNameToBrushConverter}}" />
        <Style.Resources>
            <Style BasedOn="{StaticResource LabelDotTextStyle}" TargetType="TextBlock" />
        </Style.Resources>
    </Style>

    <Style x:Key="TransparentDotStyle" BasedOn="{StaticResource BaseDotStyle}" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Style.Resources>
            <Style BasedOn="{StaticResource LabelDotTextStyle}" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{Binding Group, Converter={StaticResource GroupNameToBrushConverter}}" />
            </Style>
        </Style.Resources>
    </Style>

    <Style x:Key="DefaultTextStyle" TargetType="Border">
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Background" Value="#80000000" />
        <Setter Property="CornerRadius" Value="2" />
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="White" />
            </Style>
        </Style.Resources>
    </Style>

    <Style x:Key="ColorPickerButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="50" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="BorderBrush" Value="Gray" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <!--  选中指示器  -->
                        <Border
                            x:Name="selectedIndicator"
                            Width="8"
                            Height="8"
                            Margin="0,-3,-3,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Background="RoyalBlue"
                            CornerRadius="4"
                            Visibility="Collapsed">
                            <materialDesign:PackIcon Width="6" Height="6" Foreground="White" Kind="Check" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="RoyalBlue" />
                            <Setter TargetName="border" Property="BorderThickness" Value="2" />
                        </Trigger>
                        <Trigger Property="Tag" Value="Selected">
                            <Setter TargetName="border" Property="BorderBrush" Value="RoyalBlue" />
                            <Setter TargetName="border" Property="BorderThickness" Value="3" />
                            <Setter TargetName="selectedIndicator" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Dialogs  -->
    <Style x:Key="DialogButtonBaseStyle" BasedOn="{StaticResource IconButtonStyle}" TargetType="Button">
        <Setter Property="MinWidth" Value="88" />
    </Style>

    <Style x:Key="DialogHeaderStyle" TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type HeaderedContentControl}">
                    <Border Padding="20,18" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <materialDesign:PackIcon
                                Width="40"
                                Height="40"
                                Margin="0,0,12,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Kind="{TemplateBinding Tag}" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="{TemplateBinding Header}" />
                                <TextBlock
                                    x:Name="DescriptionText"
                                    Margin="0,3,0,0"
                                    Foreground="{DynamicResource MaterialDesignBody}"
                                    Opacity="0.68"
                                    Text="{TemplateBinding Content}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Content" Value="">
                            <Setter TargetName="DescriptionText" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="Content" Value="{x:Null}">
                            <Setter TargetName="DescriptionText" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DialogFooterBorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="20,14" />
        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
    </Style>

    <Style x:Key="DialogDividerStyle" TargetType="Border">
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="0,8,0,0" />
        <Setter Property="Background" Value="{DynamicResource MaterialDesignDivider}" />
    </Style>
</ResourceDictionary>
