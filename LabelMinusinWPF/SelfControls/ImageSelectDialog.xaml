<Window
    x:Class="LabelMinusinWPF.ImageSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="选择图片"
    Width="700"
    Height="600"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="请选择要包含在翻译中的图片：" Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
            <TextBlock Margin="0,4,0,0" Opacity="0.7" Text="已选中的图片将保留其标注数据，未选中的图片将失去标注数据" />
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
            <ListBox
                x:Name="ImageListBox"
                SelectionMode="Multiple"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border
                                        x:Name="ItemBorder"
                                        Margin="2"
                                        Padding="8,4"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Cursor="Hand">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MaterialDesignSelection}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox
                            MinWidth="150"
                            VerticalAlignment="Center"
                            Content="{Binding ImageName}"
                            IsChecked="{Binding IsSelected, Mode=TwoWay}"
                            FontSize="12"
                            IsHitTestVisible="False" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,16,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="0,0,8,0" Content="全选" Click="SelectAll_Click" Style="{StaticResource MaterialDesignFlatButton}" />
            <Button Margin="0,0,16,0" Content="全不选" Click="DeselectAll_Click" Style="{StaticResource MaterialDesignFlatButton}" />
            <Button Margin="0,0,8,0" Content="确定" Click="OK_Click" IsDefault="True" />
            <Button Content="取消" Click="Cancel_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
