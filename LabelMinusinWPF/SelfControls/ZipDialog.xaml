<Window
    x:Class="LabelMinusinWPF.ZipSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="关联压缩包"
    Width="550"
    Height="450"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock
                Text="选择要关联的压缩包："
                Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
            <TextBlock
                Margin="0,4,0,0"
                Opacity="0.7"
                Text="提示：选择“无”可取消关联，切换回当前文件夹" />
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4">
            <ListBox
                x:Name="ZipListBox"
                SelectionMode="Single"
                ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Padding="12,8"
                            Margin="4"
                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                            BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    Kind="FolderZip"
                                    Width="24"
                                    Height="24"
                                    VerticalAlignment="Center"
                                    Margin="0,0,12,0"
                                    Foreground="{DynamicResource PrimaryHueMidBrush}" />
                                <TextBlock
                                    Text="{Binding}"
                                    VerticalAlignment="Center"
                                    FontSize="14" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="确定" Click="OK_Click" IsDefault="True" Margin="0,0,8,0" />
            <Button Content="取消" Click="Cancel_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>