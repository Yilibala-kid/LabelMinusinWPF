<Window
    x:Class="LabelMinusinWPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:LabelMinusinWPF"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="{Binding CurrentProject.DisplayTitle}"
    Width="1200"
    Height="800"
    mc:Ignorable="d">
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding NewFolderTranslationCommand}" />
        <KeyBinding Key="N" Modifiers="Control+Shift" Command="{Binding NewZipTranslationCommand}" />
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding OpenTranslationCommand}" />
        <KeyBinding Key="O" Modifiers="Control+Shift" Command="{Binding OpenImageOrZipCommand}" />
        <KeyBinding Key="O" Modifiers="Control+Alt" Command="{Binding OpenImageFolderCommand}" />
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveTranslationCommand}" />
        <KeyBinding
            Key="S"
            Modifiers="Control+Shift"
            Command="{Binding SaveTranslationCommand}"
            CommandParameter="As" />
        <KeyBinding Key="F" Modifiers="Control" Command="{Binding OpenNowFolderCommand}" />
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding SelectedImage.UndoCommand}" />
        <KeyBinding Key="Y" Modifiers="Ctrl" Command="{Binding SelectedImage.RedoCommand}" />
        <KeyBinding Key="Z" Modifiers="Ctrl+Shift" Command="{Binding SelectedImage.RedoCommand}" />
    </Window.InputBindings>
    <Window.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal" />
        </Style>
        <local:EnumToBooleanConverter x:Key="EnumToBoolConverter" />
    </Window.Resources>
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid x:Name="RootGrid">
        <DockPanel Grid.ColumnSpan="2">
            <!-- 菜单栏 -->
            <Menu
                x:Name="Menu"
                materialDesign:MenuAssist.TopLevelMenuItemHeight="25"
                FontFamily="微软雅黑"
                FontWeight="Bold"
                DockPanel.Dock="Top"
                Background="{DynamicResource MaterialDesignPaper}"
                Foreground="{DynamicResource MaterialDesignBody}"
                FontSize="12">
                <!-- 1. 文件 -->
                <MenuItem Header="文件">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="File" />
                    </MenuItem.Icon>

                    <!-- 新建 -->
                    <MenuItem Header="新建">
                        <MenuItem Header="新建文件夹翻译" InputGestureText="Ctrl+N" Command="{Binding NewFolderTranslationCommand}" />
                        <MenuItem Header="新建压缩包翻译" Command="{Binding NewZipTranslationCommand}" />
                        <MenuItem.Icon>
                            <materialDesign:PackIcon Kind="FilePlus" />
                        </MenuItem.Icon>
                    </MenuItem>

                    <Separator />

                    <!-- 打开相关 -->
                    <MenuItem Header="打开翻译" Command="{Binding OpenTranslationCommand}" />
                    <MenuItem Header="打开图片/压缩包" Command="{Binding OpenImageOrZipCommand}" />
                    <MenuItem Header="打开图片文件夹" Command="{Binding OpenImageFolderCommand}" />

                    <Separator />

                    <!-- 保存相关 -->
                    <MenuItem Header="保存翻译" Command="{Binding SaveTranslationCommand}" />
                    <MenuItem Header="翻译另存为..." Command="{Binding SaveTranslationCommand}" CommandParameter="As" />

                    <Separator />

                    <MenuItem Header="打开当前文件夹" Command="{Binding OpenNowFolderCommand}" />
                </MenuItem>

                <!-- 2. 修改 -->
                <MenuItem Header="修改">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Pencil" />
                    </MenuItem.Icon>

                    <!-- 来自 WinForms 版本的具体业务功能 -->
                    <MenuItem Header="关联压缩包" />
                    <MenuItem Header="调整图集" />
                    <MenuItem Header="组别" />

                    <Separator />

                    <MenuItem Header="文本框字号" />
                    <MenuItem Header="OCR网站" />

                    <Separator />

                    <!-- 通用编辑功能 -->
                    <MenuItem Header="撤销" InputGestureText="Ctrl+Z" Command="{Binding SelectedImage.UndoCommand}" />
                    <MenuItem Header="重做" InputGestureText="Ctrl+Y" Command="{Binding SelectedImage.RedoCommand}" />
                </MenuItem>

                <!-- 3. 导出文本 -->
                <MenuItem Header="导出文本">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Export" />
                    </MenuItem.Icon>

                    <!-- 来自 WinForms 版本的内容导出 -->
                    <MenuItem Header="导出原翻译" />
                    <MenuItem Header="导出现翻译" />
                    <MenuItem Header="导出修改文档" />

                    <Separator />

                    <!-- 来自 WPF 版本的格式导出 -->
                    <MenuItem Header="导出为TXT" />
                    <MenuItem Header="导出为Excel" />
                </MenuItem>

                <!-- 4. 显示 -->
                <MenuItem Header="显示">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Monitor" />
                    </MenuItem.Icon>

                    <!-- 布局模式 -->
                    <MenuItem Header="列表与文本框" IsCheckable="True" IsChecked="True" />
                    <MenuItem Header="仅列表" IsCheckable="True" />
                    <MenuItem Header="仅文本框" IsCheckable="True" />

                    <Separator />

                    <!-- 视图选项 -->
                    <MenuItem Header="次要参数" IsCheckable="True" />
                    <MenuItem Header="列表序号" IsCheckable="True" IsChecked="True" />
                    <MenuItem Header="列表组别" IsCheckable="True" IsChecked="True" />
                    <MenuItem
                        x:Name="PicShowIndex"
                        Header="图片序号"
                        IsCheckable="True"
                        IsChecked="{Binding IsPictureIndexVisible}" />
                    <MenuItem
                        x:Name="PicShowText"
                        Header="图片文本"
                        IsCheckable="True"
                        IsChecked="{Binding IsPictureTextVisible}" />

                    <Separator />

                    <!-- 缩放与全屏 (WPF) -->
                    <MenuItem Header="全屏" />
                    <MenuItem Header="放大" />
                    <MenuItem Header="缩小" />
                </MenuItem>

                <!-- 5. 高级 -->
                <MenuItem Header="高级">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Cogs" />
                    </MenuItem.Icon>
                    <MenuItem Header="设置" />
                    <MenuItem Header="插件管理" />
                </MenuItem>

                <!-- 6. 帮助 -->
                <MenuItem Header="帮助">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Help" />
                    </MenuItem.Icon>
                    <MenuItem Header="官网" />
                    <MenuItem Header="关于" />
                </MenuItem>
                <ToggleButton
                    Width="40"
                    VerticalAlignment="Center"
                    Cursor="Hand"
                    ToolTip="切换深色模式"
                    Checked="DarkMode_Checked"
                    Unchecked="DarkMode_Unchecked">
                    <materialDesign:ToggleButtonAssist.OnContent>
                        <materialDesign:PackIcon Kind="WeatherNight" />
                    </materialDesign:ToggleButtonAssist.OnContent>
                    <materialDesign:PackIcon Kind="WeatherSunny" />
                </ToggleButton>
            </Menu>
            <!-- 顶栏 -->

            <!-- 主界面 -->
            <Grid ShowGridLines="False" ToolBar.OverflowMode="Never">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <!-- 左侧图片与底栏 -->
                <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}">
                    <!-- 工具栏覆盖住 -->
                    <materialDesign:DrawerHost x:Name="ImageAreaDrawer">
                        <materialDesign:DrawerHost.LeftDrawerContent>
                            <StackPanel>
                                <TextBlock Text="图片工具箱" />
                                <Button Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}" Content="关闭工具栏" />
                            </StackPanel>
                        </materialDesign:DrawerHost.LeftDrawerContent>
                        <!-- 图片 -->
                        <DockPanel>
                            <Border Height="35" DockPanel.Dock="Top" Background="{DynamicResource MaterialDesignPaper}">
                                <Grid Margin="10,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <Button
                                            Style="{StaticResource IconButtonStyle}"
                                            Tag="Toolbox"
                                            Content="工具箱"
                                            ToolTip="打开工具箱"
                                            Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
                                            CommandTarget="{Binding ElementName=ImageAreaDrawer}" />
                                        <RadioButton
                                            x:Name="SEEMode"
                                            Style="{StaticResource IconRadioButtonStyle}"
                                            GroupName="AppModes"
                                            IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static local:AppMode.See}}"
                                            Tag="Eye"
                                            Content="浏览"
                                            ToolTip="浏览图片" />

                                        <RadioButton
                                            Style="{StaticResource IconRadioButtonStyle}"
                                            GroupName="AppModes"
                                            IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static local:AppMode.LabelDo}}"
                                            Tag="FileEdit"
                                            Content="标记"
                                            ToolTip="标记图片" />

                                        <RadioButton
                                            x:Name="OcrBtn"
                                            Style="{StaticResource IconRadioButtonStyle}"
                                            GroupName="AppModes"
                                            IsChecked="{Binding CurrentMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter={x:Static local:AppMode.OCR}}"
                                            Tag="ImageSearch"
                                            Content="识别"
                                            ToolTip="内容识别" />

                                        <StackPanel>
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CurrentMode}" Value="{x:Static local:AppMode.OCR}">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>

                                            <ComboBox
                                                Width="150"
                                                Height="35"
                                                Margin="10,0,0,0"
                                                materialDesign:HintAssist.Hint="选择网站">
                                                <ComboBoxItem Content="百度" />
                                                <ComboBoxItem Content="谷歌" />
                                                <ComboBoxItem Content="必应" />
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <Button
                                            Style="{StaticResource IconButtonStyle}"
                                            Click="OpenImageReview_Click"
                                            Tag="Compare"
                                            Content="图校"
                                            ToolTip="图片对比" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ToolBar Height="36" DockPanel.Dock="Bottom">
                                <Button Padding="4" ToolTip="适应视图" Click="FitToPage_Click">
                                    <materialDesign:PackIcon Kind="FitToPage" />
                                </Button>
                                <Button Padding="4" ToolTip="适应宽度" Click="FitWidth_Click">
                                    <materialDesign:PackIcon Kind="ArrowExpandHorizontal" />
                                </Button>
                                <Button Padding="4" ToolTip="适应高度" Click="FitHeight_Click">
                                    <materialDesign:PackIcon Kind="ArrowExpandVertical" />
                                </Button>

                                <ToggleButton
                                    Padding="4"
                                    Style="{StaticResource IconToggleButtonStyle}"
                                    Tag="ArrowHorizontalLock"
                                    IsChecked="{Binding IsYLocked, ElementName=PicView, Mode=TwoWay}"
                                    ToolTip="横向移动" />

                                <ToggleButton
                                    Padding="4"
                                    Style="{StaticResource IconToggleButtonStyle}"
                                    Tag="ArrowVerticalLock"
                                    IsChecked="{Binding IsXLocked, ElementName=PicView, Mode=TwoWay}"
                                    ToolTip="纵向移动" />

                                <Separator />
                                <StackPanel Margin="5,0">
                                    <materialDesign:PackIcon Margin="0,0,10,0" VerticalAlignment="Center" Kind="ImageMultiple" />
                                    <ComboBox
                                        Width="200"
                                        VerticalAlignment="Center"
                                        materialDesign:HintAssist.Hint="选择图片"
                                        ItemsSource="{Binding ImageList}"
                                        SelectedItem="{Binding SelectedImage}"
                                        DisplayMemberPath="ImageName"
                                        FontSize="12" />
                                </StackPanel>
                                <StackPanel>
                                    <Button Padding="4" ToolTip="上一张" Command="{Binding PreviousImageCommand}">
                                        <materialDesign:PackIcon Kind="ArrowUp" />
                                    </Button>
                                    <Button Padding="4" ToolTip="下一张" Command="{Binding NextImageCommand}">
                                        <materialDesign:PackIcon Kind="ArrowDown" />
                                    </Button>
                                </StackPanel>
                                <Separator />
                                <Button
                                    Style="{StaticResource IconButtonStyle}"
                                    Command="{Binding OpenNowFolderCommand}"
                                    Tag="FolderMultipleImage"
                                    Content="文件夹"
                                    ToolTip="打开工作文件夹" />
                            </ToolBar>
                            <!-- 图片！！！！！ -->
                            <Grid x:Name="ImageParent" ClipToBounds="True">
                                <local:ImagewithLabelShow
                                    x:Name="PicView"
                                    DataContext="{Binding SelectedImage}"
                                    IsIndexVisible="{Binding IsChecked, ElementName=PicShowIndex}"
                                    IsTextVisible="{Binding IsChecked, ElementName=PicShowText}"
                                    OnlySEE="{Binding IsChecked, ElementName=SEEMode}"
                                    IsScreenShotMode="{Binding IsChecked, ElementName=OcrBtn}"
                                    IsSyncRequired="False" />

                            </Grid>
                        </DockPanel>
                    </materialDesign:DrawerHost>
                </Border>

                <GridSplitter
                    Grid.Column="1"
                    Width="5"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />
                <!-- 右侧列表与文本 -->
                <Grid Grid.Column="2" Background="RoyalBlue">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DataGrid
                        x:Name="LabelDataGrid"
                        Margin="4"
                        Style="{StaticResource MaterialDesignDataGrid}"
                        ItemsSource="{Binding SelectedImage.ActiveLabels}"
                        SelectedItem="{Binding SelectedImage.SelectedLabel, Mode=TwoWay}"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        SelectionMode="Single"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="FontWeight" Value="Bold" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="60"
                                Header="序号"
                                Binding="{Binding Index}"
                                IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <!-- 高级文本编辑 -->
                            <DataGridTemplateColumn Width="*" Header="内容">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox
                                            Padding="2"
                                            VerticalContentAlignment="Top"
                                            Text="{Binding Text, UpdateSourceTrigger=LostFocus}"
                                            TextWrapping="Wrap"
                                            AcceptsReturn="True"
                                            BorderThickness="0"
                                            Background="White"
                                            Foreground="Black"
                                            Style="{x:Null}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Width="60" Header="组" Binding="{Binding Group}" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <!-- 文本框 -->
                    <TextBox
                        Grid.Row="1"
                        HorizontalContentAlignment="Left"
                        VerticalContentAlignment="Top"
                        Text="{Binding SelectedImage.SelectedLabel.Text, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Background="{DynamicResource MaterialDesignPaper}"
                        Foreground="{DynamicResource MaterialDesignBody}"
                        FontSize="16"
                        TextAlignment="Left" />
                </Grid>
            </Grid>
        </DockPanel>
        <!-- 图校界面 -->
        <local:ImageReView
            x:Name="FullScreenReview"
            Grid.ColumnSpan="2"
            Visibility="Collapsed"
            ExitClicked="FullScreenReview_ExitClicked" />
        <!-- 在底部显示消息 -->
        <materialDesign:Snackbar
            x:Name="MessageShow"
            MaxWidth="1600"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Background="RoyalBlue"
            Foreground="White"
            FontWeight="Black"
            FontFamily="微软雅黑"
            FontSize="14"
            MessageQueue="{Binding MainMessageQueue}" />
    </Grid>
</Window>
